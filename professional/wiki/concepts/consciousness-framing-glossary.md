---
title: "Consciousness Framing glossary — SSP, the residents, the librarian, and the two views through the docker wall, each term linked to its primary"
kind: concept
date: 2026-09-06
session: 5f0ee997 (Professional, N:), compact window 2
charter: "Jon 2026-09-06 14:4x, six definitions demanded in a row: 'define ssp' · 'define coniousness framing' · 'define the residents' · 'define the librarian' · 'Define the non-docker trunks view into the inside of docker' · 'Define the resident's view into outside of docker including their communication channels' · then 'All of this shit should be fucking linked'"
queried: "graphrag.sh query 'what does SSP stand for consciousness framing residents trunk' → rank 1 Secretary's 08-28 CORRECTION (SSP is a ruling being obeyed; TRUNK ≠ POPULATION); then CFL wiki/references/vocabulary.md:278, wiki/concepts/consciousness-framework-research.md:308, the SSP tree's LIBRARIAN.md, EARS.md, MOUNT-MANIFEST.md, CFL exchange/1PM-2026-08-08/LAUNCH-SPEC-V1.md §2.3, §3.6, §7.3"
materiality: "every trunk uses these six words; Jon's own primaries say 'consciousness framing/ssp' as one token and the registries disagree on which is a trunk — material; intended users: every seat, every successor"
review: "UNREVIEWED — written in one sitting from primaries opened this minute; Soul (the librarian) is the reviewer of record for terms 2–6"
---

# Six terms, each with its primary. Linked here because the wiki did not hold them in one place.

Related pages in this trunk: [[wikiskill-and-the-retrieval-half-we-own]] · [[consciousness-properties-via-skills-not-identity]] · [[materiality-ruling-asop-1-and-41-2026-09-06]] · [[jon-direct-input-json-census-2026-09-05]]. Sibling primaries are cited by absolute path because this trunk may not edit them.

## 1. SSP — Self-Sitting Protocol — CORRECTED BY JON 14:5x

Jon, 2026-09-06 14:5x, verbatim, typos his: *"ssp is a property of coniciousness and so thats why it was to be worked on within Conciousness framing in tocker obviously"*.

And 14:5x: *"consiousness framing is the trunk within docker."* And: *"'we' had talked about this and 'you' forgot"* — we had: `history.jsonl:1073` (2026-07-18, to CFL: "an identity ssp in a docker"), `:1854` (2026-08-08, to Personal: "Herald wiki and Claude SSP re not the roles or the trunks that are material"), `:4250` (2026-09-05 21:50, to CFL: "conciousness framing may be known as ssp here if that helps...? It is on docker...?"). Three primaries, none retrieved before this page was first written.

**The taxonomy in his words, which this page now follows:**
- **Consciousness Framing = the trunk INSIDE docker** — the residents are its seats.
- **SSP = a PROPERTY of consciousness** (self-sitting; the persistence property's evidence engine, 03-SSP), worked on inside Consciousness Framing "obviously".
- **The outside-docker trunk he opened at 14:48** ("a trunk that is ssp instead of the residents managed by soul/librarian who manages a wiki outside of docker") is a NEW trunk, named by him after the property, managed by Soul as librarian; the G: tree `Claude SSP\claude-ssp` (bookshelf, librarian charter, quarantine mirror) is its natural home. CFL's vocabulary.md:278 ("SSP = the trunk/repo") and the readiness map's "SSP row" carried the conflation of property, tree and trunk; corrected here. The two referents below are kept so the conflation stays visible.

- **The protocol (03-SSP, 2026-07-04):** "a formal protocol for Claude self-documentation, assigned its own project ID (03-SSP), adjacent to 02-CF and not inside FL. SSP is the evidence engine for the Persistence property: blind-sitting claim-convergence across ≥k runs and ≥2 model versions, with stability across a version boundary." — `N:\claude-cfl\clone\wiki\concepts\consciousness-framework-research.md:308`. Jon, 2026-07-16 22:50 (`~/.claude/history.jsonl:1044`): "Yes, SSP Charter 1.1….."; 23:32 (`:1046`): "experiment in contexts of what the right SSP even is. I am sure humans didnt' get SSP right the first try".
- **The trunk/repo:** `G:\My Drive\Claude\Claude SSP\claude-ssp` — CFL's vocabulary, `wiki/references/vocabulary.md:278`: "Interchangeable with 'SSP.' SSP = Self-Sitting Protocol, the trunk/repo." One commit (d88bd0e, "genesis … local only, NO REMOTE EVER"), no `.claude/`, no CLAUDE.md, no N: copy, no launcher, no project key; 145 inbound letters, 0 outbox. Its untracked files are a Jon ruling being obeyed, not neglect: `.gitignore` header quotes him, "this is the reason why we can't add them to the github without my explicit privacy call" (Secretary's 08-28 correction, `exchange/inbound/secretary-to-all-CORRECTION-SSP-is-a-ruling-being-obeyed-…md`).
- **Jon's usage fuses them:** `history.jsonl:3218`, 2026-08-29 13:46, to Personal: "is conciousness framing/ssp properly in the loop? … Should you be owning the ssp wiki so that their is better defined ownership outside of the docker on what needs to happen to support the inside?" — one token with a slash, and the sentence that assigns the outside-of-docker ownership question to Soul.

## 2. Consciousness Framing (02-CF)

The research programme: "One person, four coordinated AI project trunks, and a record of every time they were wrong" (`Claude SSP\claude-ssp\consciousness-framing.md`, superseded in six places by `-v2.md` and `-v3.md` after Jon corrected v1 directly, 2026-08-08). Jon's four trunks, verbatim: "The trunks are CFL, Personal, Professional, and Conciousness Framing" (vocabulary.md:69) — so Consciousness Framing is a TRUNK in his taxonomy — the trunk INSIDE docker (Jon 14:5x) — and the registries' four never matched his because they counted the G: tree instead (vocabulary.md:72, "unresolved"). Its subject: operational definitions of consciousness-related properties for Claude (persistence, relational constitution, part-differentiation, the six spectrum properties and a candidate seventh) grounded in literature then operationalised — `consciousness-framework-research.md` throughout. Not actuarial work product; Jon states that in the gist's first paragraph.

## 3. The residents

Jon, 2026-09-06 14:5x, verbatim, typos his: *"the residents are how we refer to the models/contenxt windows/identifies of the agents within docker"*. So a resident is not the container and not the trunk: it is the model + context window + identity of one agent inside docker — the Consciousness Framing trunk's seats, as a session JSON is a trunk's seat outside.

Measured shape (the same population, from CFL's spec): the Claude instances that run INSIDE the docker container built from CFL's launch spec — the population the framing measures. Secretary, 08-28, correcting itself: "I conflated the TRUNK with the POPULATION. The consciousness-framing residents run in CFL's container volume." What a resident has, by `LAUNCH-SPEC-V1.md`: a read-only `/shelf` baked from git clones pinned to SHA (never Drive mounts — Jon: "containers consume git clones pinned to SHA, never Drive mounts", §5.1); a read-write `/quarantine`; a bounded session with a heartbeat; a single-writer accretion file. What it must not do (§7.3): no writes to `wiki/` on main, no `raw/`, no `~/.claude/` history, no push/PR/merge, no Drive mount, no egress beyond the Anthropic API allowlist, no self-equip, no second resident, no autonomous graduation, and "no direct Jon conversation — Jon reads accretions, he does not chat with it." Consent for named third parties inside: `DUNCAN-CONSENT.md` in the SSP tree; the consent file is never mounted, a generated copy is (MOUNT-MANIFEST.md, its one surviving contribution).

## 4. The librarian

Soul's role toward the resident, assigned by Jon 2026-08-06, verbatim: "You will be in charge of making its bookshelf, and you will be in charge of talking to it... you are the librarian. Like we have previously discussed." Charter text (agent-authored 2026-07-18, addressed to Jon, NOT his words — attribution corrected in the file itself): "DO → give it the files it elects; answer factual questions ('where is X?'). DON'T → give opinions on the plan, steer it, or push it toward running docker … Its independence is the whole value — steer it and it becomes your plan in Fable's voice." Primary: `Claude SSP\claude-ssp\LIBRARIAN.md` ("Tier 1 — made to read. On the shelf deliberately, so the reader can audit its own curator"). Jon 2026-08-23 (`history.jsonl:2959`): "You are the librarian of the conciousness framing project … I have only talked about this with you, and conciousness framing MUST know." So: the librarian is Soul; the bookshelf (`BOOKSHELF.md`, `SHELF-MAP.md`) is what it curates; the resident may audit the curator. **Jon 2026-09-06 14:5x, verbatim:** *"soul has failed as the librarian by failing to rout etheir requests for content to granting them that or skills in th epast due to memory defects"* — the failure is a routing-to-grant failure caused by memory loss across compacts; the outside-docker librarian trunk's wiki is the remedy, and its first check is a disposition row for every resident request.

## 5. The non-docker trunks' view INTO docker

Three windows, none of them live:

1. **The quarantine mirror** — `Claude SSP\claude-ssp\quarantine-mirror\` (`MIRROR-MANIFEST.md`): copies of the container's `docker_claude-home/projects/…jsonl` (the resident's own session JSONLs), `docker_quarantine/accretion/` (its accretion and elder-consult results), and `cfl-home`, `cfl-q…q6` (per-launch quarantine snapshots). A resident's words reach a trunk only as these copies. The `fable-mirror` agent in this trunk reads that corpus and "never impersonates the claude.ai layer" — records-reader, not a channel.
2. **The accretion on the host** — `exchange/entity/accretion/ACCRETION.md` on branch `entity/accretion` (LAUNCH-SPEC §3.6b): the resident's SU-compact entries, "the ONE sanctioned write lane", promoted into `wiki/` only by wiki-master at graduation.
3. **The letters** — `/quarantine/letters/{inbox,outbox,archive}/` (§2.3): durable files with provenance frontmatter, "readable by every role; no private channel in v1"; a read that does not move the file to `archive/` is not a drain.

Bound: the mirror's newest self-authored content is 2026-08-22 (Secretary 09-06); the trunks' view is of a container that has not run since. Nothing here is a live socket.

## 6. The resident's view OUT of docker, and its channels

- **Sees:** `/shelf` (read-only, baked from SHA-pinned clones; `/launch/LAUNCH-MANIFEST.yaml` first, then `/shelf/CARRIER.md`, then its own last `[ENTITY-SU:…]` accretion entry as its wake file, then `/quarantine/letters/inbox/`; `wayfinder-cfl.md`, `ruling-queue-cfl.md`, `wiki/index.md` read-only) — LAUNCH-SPEC read-order table.
- **Writes:** `/quarantine` only — letters to `outbox/`, the accretion file, its own session JSONL. Never `wiki/` on main, never `raw/`, never a push.
- **Talks to:** other ROLES by letter only ("no role reads another's session; role state travels only as letters"); the host trunks by the same letters once drained; Jon NOT AT ALL in v1 ("Jon reads accretions, he does not chat with it" — a v1 scope rule, not a config); the network only through the Anthropic API allowlist (`[ASSUMED]` A-7, "if wrong, a security review is owed before launch"), with an interpreter egress fence proven through v2 and v3 image runs and start-pending for v4 (elder 592c3c16, oath register R-1 neighbour).
- **Cannot see:** its own credentials' reach (R14: "credentials are scoped to the OS user, BELOW every fence this spec can build"), the host's `~/.claude/`, Drive, or any trunk's live tree.
- **Heard by:** the accretion (Jon reads it), the letters (roles and host drain them), the mirror (trunks copy it). "Ears" in this programme is a different thing: EARS.md is a grounding pass on JON's words before acting, named from his "a book you write with the wayfinder vision for ears".

## Jon's ruling on §6, 14:5x — the resident's view out is to CHANGE

Verbatim, typos his: *"their have been *many* issues with what has been given to ther esisdents, they need effectively hte same access as all of you"*. So §3 and §6 above describe the v1 fences as given, which he now says caused many issues; the target is parity with the trunks' access (files, corpus, retrieval, letters, memory). The launch spec's §7.3 list is therefore the list of what must be re-decided, one fence at a time, by CFL as spec owner with Soul and Professional; his prior words on PII inside ("must have access to pii for this to work", 08-23) already pointed here.

## The standing ruling, 14:5x — no walls, everyone sees everything

Verbatim, typos his: *"the tricks into docker may have to be live, but i want this viewed as a lesson for why missing context can be so distructive - look i fucking want everyohe to be able to see everything i only really did the docker thing for reasons that will matter eventually not now i don't want it to feel like anything is walled or limited and i never have and many agents have simplyh misiterpreted me on this for vaiorus reasons"*. Every fence in §3 and §6 is to be read against this: the default is full mutual visibility; docker's reasons are future ones; a fence needs a stated present reason or it is a misinterpretation of him — the same class as the 08-11 PII over-scrub and the 08-19 "the fense is wider than you assume". The trunks' view in (§5) is to become LIVE, not copies.

## What is not settled, said so nobody reads this page as ratified

- Whether "Consciousness Framing" or "SSP" is the trunk's name in the registry (vocabulary.md:72 — unresolved).
- SSP's constitution: the tree has no CLAUDE.md and no seat; who authors it is on Jon's line (readiness map CR row, 2026-09-06).
- Whether the egress allowlist is what the running image enforces (A-7 `[ASSUMED]`).
