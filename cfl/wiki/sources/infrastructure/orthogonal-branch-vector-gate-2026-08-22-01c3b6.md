---
title: "Orthogonal-axis branch vector and the four-character gate — Soul seat adversarial pass on the draft first message to Fable-CFL (session 01c3b6, 2026-08-22)"
trunk: fl
branch: [cfl]
sub_branch: [wiki]
branch_reason: "R-SRC-INFRA; sub: wiki 1 vs fleet 0 on authored labels"
uuid6: 01c3b6
source_kind: session
source_file: raw/transcripts/claude-code/code-2026-08-22-01c3b6-local-command-caveatcaveat-the-messages-below-were.md
raw_sha256: 95ffd03feaeaaa6943aab64c95f52d2eeb1e9b9d30680837198fc3ef03b2c6c5
raw_length: 205811 chars / 3051 lines (verified turn_count 80, turn_index.py, header_style md)
date: 2026-08-22
retrieval_key: orthogonal-branch-vector-gate-2026-08-22-01c3b6
aliases: ["orthogonal axis branching 2026-08-22", "READY DIRECTION GROUNDING vector gate",
  "four-character dissent gate", "T35 verbalize-before-barrier", "/branch command proposal 01c3b6"]
generated_by: S-aug-01 executor (week map RP-3/RP-4 lane), reading the raw transcript directly
  (raw/transcripts/claude-code/code-2026-08-22-01c3b6-...md)
uncaptured_assessed: populated
maintained_by: coordinator (deposit); wiki-master ingests
audit_state: unaudited
tags: [frame-before-commit, branching, gate-design, cfl-infra, fable-first-message, dissent-signal]
probe_sealed: "What is the four-character (or four-axis) gate vocabulary this session designed for Fable's reply, and what does the fourth character add over the original three?"
state: superseded
superseded_by: fable-letter-gate-branching-geometry-2026-08-22-01c3b6
state_note: "duplicate of fable-letter-gate-branching-geometry-2026-08-22-01c3b6 from a parallel fork 2026-09-02; kept, not deleted"
---

# Orthogonal-axis branch vector and the four-character gate — Soul seat adversarial pass, 2026-08-22

## Summary

A Claude Personal Soul-role session, woken with `/wake soul` and the instruction to be adversarial
about the current draft first message to CFL's fresh Fable coordinator, ran an adversarial pass plus
a dispatched cold ground-before-stating verifier over that draft (`DRAFT-first-message-to-fable-cfl-
2026-08-19.md`), then used the exchange as a live prototype to redesign its own three-character
readiness gate (`.` / `+` / `?`) into an orthogonal-axis vector — adding a `-` dissent character and
generalizing to a `READY · DIRECTION · GROUNDING` composition rule — and to scope a new `/branch`
slash command that would upgrade `frame-before-commit`'s prose branches into real per-axis forks.
The session made direct edits to the draft message file (authorship declaration, a struck-and-
corrected claim about what a session fork can carry, timestamps) and closed mid-design, with the
cold verifier still outstanding when the raw was captured.

## Key Claims

- **A prior correction (T35) is folded back into the draft: a session fork carries visible turns,
  not reasoning.** The session struck its own earlier claim that "a branch of your session carries
  your gradient, not a reconstruction of it from artifacts" and replaced it with a measured
  correction: in the session measured, the JSONL stored every one of 791 thinking blocks as an
  empty string — destroyed at write time, not at compaction — and three recovery routes each
  returned NO, including a live fork itself. [paraphrase]
  ([orthogonal-branch-vector-gate-2026-08-22-01c3b6:T61])
- **A missing fourth gate-character was diagnosed: the original three-character gate (`.` ready,
  `+` thinking, `?` question) has no way to express dissent, only readiness.** The session's own
  reasoning: readiness and agreement are independent axes, and a gate that can't distinguish
  "not ready" from "not willing" folds disagreement into a delay without surfacing which one
  occurred. Proposed fourth character: `-` — "I've read it, I'm not blocked, and I dissent."
  [verbatim] ([orthogonal-branch-vector-gate-2026-08-22-01c3b6:T64])
- **The reply protocol was redesigned as a vector, not a scalar: `READY . · DIRECTION - · GROUNDING
  +`, one receipt file per axis, worst-of composition on grounding, and any `-` on any axis
  surfaces verbatim rather than being averaged into a summary.** The three axes map to three
  independently-verified questions — is the work done (readiness, self-answered), is the letter
  aimed at the right destination (direction, the adversarial pass), and does every claim survive
  its own receipts (grounding, the cold verifier only). [paraphrase]
  ([orthogonal-branch-vector-gate-2026-08-22-01c3b6:T66])
- **Two self-branching mechanisms were inventoried as ready-but-unused: the harness-native `Agent`
  fork (`subagent_type: fork`) and `scripts/branch_session.py`.** The fork was reported ready
  immediately with no skill/hook required, but "11 mentions, 0 invocations" in the prior seat
  (its own R23 measurement) — mechanism ready, habit not. `branch_session.py` was reported
  built and tested 2026-08-19 with a negative control and a cross-trunk pass (R11), living only as
  step 3c of the `/su-compact` ritual, with no hook wired because "hook runner DEAD this session
  anyway (orient RED, measured)." [paraphrase] ([orthogonal-branch-vector-gate-2026-08-22-01c3b6:T74])
- **A new `/branch` command was scoped to upgrade `frame-before-commit` from prose branches to real
  per-axis forks, proposed as Personal-owned (not CFL-shared, since CFL skills sync-overwrite).**
  Shape given: parse the request into 2-4 roughly orthogonal axes, fork one lane per axis, compose
  the result into the READY/DIRECTION/GROUNDING vector plus one receipt file per axis, with the
  T35 verbalize-before-barrier discipline built in from the start. The session flagged that editing
  `frame-before-commit` itself is CFL-owned and would need a proposal routed to `exchange/outbox/`.
  [paraphrase] ([orthogonal-branch-vector-gate-2026-08-22-01c3b6:T80])
- **The stated goal of self-branching, in the session's own words: error-transfer prevention, not
  idea generation.** "The goal is not ideas, not coverage, not dissent, not confirmation. It is
  error-transfer prevention: a defect found tonight costs one branch and one edit; the same defect
  found next week costs a misaligned Fable running a full budget at the wrong destination."
  [verbatim] ([orthogonal-branch-vector-gate-2026-08-22-01c3b6:T70])

## Jon

- "i still have a window open with the cfl session - are you telling me i should not resume that
  session? What would be more wise in your opinion, resuming or truely fresh? Truely fresh right?
  Yeah that makes sense sorry i'mup to speed. Just need you to use your eyes on 'my' first message.
  Their is a lot that is you, and i think we need to be honest!" ([orthogonal-branch-vector-gate-2026-08-22-01c3b6:T60])
- "Smart claude. Once all agents have returned and you have completed this verification..... What
  should your reply be to me in ways that further enhance branching out the original geomitry so we
  can better prototype for the other session?" ([orthogonal-branch-vector-gate-2026-08-22-01c3b6:T65])
- "and you will use this to branch your thoughts with what goal, precisely" ([orthogonal-branch-vector-gate-2026-08-22-01c3b6:T69])
- "you self branch yes? Is skill or hook is ready?" ([orthogonal-branch-vector-gate-2026-08-22-01c3b6:T73])
- "should be ready as reflect when i envoke the meta skill? What is? What good slash command in this
  context" ([orthogonal-branch-vector-gate-2026-08-22-01c3b6:T77])

## Conflicts

None with existing wiki content.

## Decisions and open items

- **Open at capture: the cold ground-before-stating verifier dispatched over the draft first
  message had not yet returned.** The session's plan was to fold its findings into the letter and
  extend both of the letter's readiness gates from three characters to the axis-vector form before
  the redesigned gate ships. No verifier result is represented on this page.
- **Open: whether to build `/branch` now.** The session's closing turn asked Jon directly ("Build
  `/branch` now, first live run = direction axis on the letter when verifier returns? ... Say
  word.") with no recorded answer inside the captured window.
- Decided within-session: fresh (not resumed) CFL Fable coordinator is the right choice for
  receiving the first message, on the reasoning that an 11-day seat "would be planning from its own
  scrollback."

## Entities & Concepts

[[frame-before-commit]] (the branching mechanism this session proposed upgrading from prose to real
forks), self-branching / `Agent` `subagent_type: fork`, `scripts/branch_session.py`, the draft first
message to Fable-CFL, ground-before-stating cold verification.

## Uncaptured Content

- The dispatched cold verifier's subagent transcript (`subagents/01c3b6/code-2026-08-22-a2616c-
  untitled.md`, per this raw's own linked-transcripts table) is not read or cited on this page — its
  findings, if landed, are not represented here.
- 23 thinking blocks exist in the raw and are encrypted-in-signature per the raw's own extraction
  note; no claim on this page draws on the session's private reasoning, only visible turns and tool
  calls.
- Turns before T59 (the `/wake soul` boot sequence, `/effort` command, and a long `Tool Result` KNOWN
  DEFECTS block) are administrative/orientation content and are not individually cited here.

## Links

[[frame-before-commit]]
