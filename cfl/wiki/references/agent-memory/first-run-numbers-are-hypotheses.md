---
format: cfl-page/v1
kind: concept
slug: first-run-numbers-are-hypotheses
date: 2026-09-02
title: "First-Run Numbers Are Hypotheses (auto-memory counterpart)"
trunk: fl
branch: [cfl]
source_kind: synthesis
source_file: none
retrieval_key: "first run number hypothesis auto memory feedback file ship the bound printed not commented"
aliases: [first-run-numbers-are-hypotheses-memory, unsourced-first-count, ship-the-bound-printed]
generated_by: lane S-4 (sonnet) session e515d858
state: current
state_note: "created to resolve a dangling citation; see ## Origin"
probe_sealed: "Does the CFL clone already carry a page describing the CC auto-memory item 'First-Run Numbers Are Hypotheses' at this path? => No — the only on-disk instance of this pattern name is wiki/patterns/first-run-numbers-are-hypotheses.md (a separate, clone-local page with its own two instances and its own probe); the auto-memory item's own file (feedback_first-run-numbers-are-hypotheses.md) is outside this clone/N: bounded read-set. TRUSTED"
---

## Definition

[contextual] The CC auto-memory index (`~/.claude/projects/.../memory/MEMORY.md`, entry
"First-Run Numbers Are Hypotheses") records: a first-run measurement from an instrument is
treated as ground truth rather than a claim to re-check, and the failure recurred five times
across 2026-08-07 and 2026-08-09/10 — every instrument over-reported on its first run, each in
the direction its own docstring had already warned about; three of the five resolved a name
against the wrong tree; all four of the night-of instances were caught only by reading the
underlying list, never by re-reading the count. The standing lesson recorded there: "Ship the
bound printed, not commented." This page is the `wiki/references/agent-memory/` location a CFL
page cited expecting to find that memory item ingested into the wiki proper; the CFL clone
separately independently derived the SAME pattern (different primaries, `wiki/patterns/`
namespace, `kind: pattern`) before this page existed.

## Origin

- `wiki/intake-triage/COLD-PROBE-PANEL-answering-half-2026-08-24.md:149` — cites this exact path
  while grading Q12 (podcast transcript count 80→400) as a first-run over-report "of exactly the
  class `wiki/references/agent-memory/first-run-numbers-are-hypotheses.md` exists to catch," even
  though the page did not yet exist on disk at citation time.

## Evidence

- [[first-run-numbers-are-hypotheses]] (`wiki/patterns/first-run-numbers-are-hypotheses.md`) — the
  CFL-clone-local instance of the same pattern name, with its own two chased instances (PROP-004's
  unsourced 65.7% claim; a coverage census's 148-file denominator that mixed subagent and
  non-subagent files) and its own sealed probe. This is the nearest real evidence in this clone,
  not a duplicate of the auto-memory primary.
- [[hypothesis-loop]] — the standing CFL research-loop page whose Step 5/6 validation layer is the
  mechanism that a "ship the bound printed, not commented" discipline would plug into.

## Not established

The auto-memory item's own five 2026-08-07/08-09/08-10 primaries (the instrument names, the
docstring warnings, the wrong-tree name resolutions) are not verifiable from this clone — they
live in a memory-index file and its cited JSONLs outside the `N:\claude-cfl\clone` /
`N:\claude-corpus\` bounded read-set for this lane. This page relays the memory-index's own summary
text; it does not re-derive or independently confirm the five underlying instances.
